<?php

/**
 * Mavenbird
 *
 * NOTICE OF LICENSE
 *
 * This source file is subject to the Mavenbird.com license that is
 * available through the world-wide-web at this URL:
 * https://www.mavenbird.com/LICENSE.txt
 *
 * DISCLAIMER
 *
 * Do not edit or add to this file if you wish to upgrade this extension to newer
 * version in the future.
 *
 * @category    Mavenbird
 * @package     Mavenbird_Blog
 * @copyright   Copyright (c) Mavenbird (https://www.mavenbird.com/)
 * @license     https://www.mavenbird.com/LICENSE.txt
 */

use Mavenbird\Blog\Block\Frontend;
use Mavenbird\Blog\Model\Post;
use Magento\Framework\Escaper;

/** @var Frontend $block */
/** @var Escaper $escaper */
?>
<?php if ($block->getBlogStyle() == '1'): ?>
<div class="post-list-content" id="mbblog-list">
    <?php $postCollection = $block->getPostCollection(); ?>
    <?php if ($postCollection && $postCollection->getSize()) : ?>
        <div class="mb-post-list
            <?= $block->isGridView() ? /* @noEscape */ 'grid products-grid' : '' ?><?= $block->isGridView() ? ' grid-column-' . $block->getBlogModeGridView() : '' ?>">
            <?php /** @var Post $post */
            foreach ($postCollection as $post) : ?>
                <div class="post-list-item <?= $block->isGridView()
                                                ? ' post-item-grid  ' : ' post-item-list  '; ?>">
                    <?php if ($post->getImage()) : ?>
                        <div class="post-image">
                            <a href="<?= $escaper->escapeUrl($post->getUrl()) ?>">
                                <img class="img-responsive"
                                    src="<?= $escaper->escapeUrl($block->resizeImage($post->getImage(), '1000x')) ?>"
                                    alt="<?= $escaper->escapeHtml($post->getName()); ?>" />
                            </a>
                        </div>
                    <?php endif; ?>
                    <div class="post-info">
                        <h2 class="mb-post-title">
                            <a class="post-link-title" title="<?= $escaper->escapeHtml($post->getName()); ?>"
                                href="<?= $escaper->escapeUrl($post->getUrl()) ?>">
                                <?= $escaper->escapeHtml($post->getName()) ?>
                            </a>
                        </h2>
                        <div class="mb-post-info">
                            <?=
                            /** @noEscape */
                            $block->getPostInfo($post) ?>
                        </div>
                        <?php if ($block->getShortDescription()) : ?>
                            <div class="post-short-description">
                                <p><?=
                                    /** @noEscape */
                                    $block->maxShortDescription($post->getShortDescription(true)) ?></p>
                            </div>
                        <?php endif; ?>
                        <?php if ($block->getShowShareButtons()) : ?>
                            <div class="details-sharing">
                                <div id="sharing">
                                    <div class="share-col-left">
                                        <h5><?= $escaper->escapeHtml(__('Share this post')) ?></h5>
                                    </div>
                                    <div class="share-col-right">
                                        <div class="post-sharing-button">
                                            <?php foreach ($block->getShareLinks($post) as $share) : ?>
                                                <?php if ($share['enabled']) : ?>
                                                    <a class="sharing-icon"
                                                        href="<?= $escaper->escapeUrl($share['url']) ?>"
                                                        target="_blank"
                                                        rel="noopener"
                                                        title="<?= $escaper->escapeHtmlAttr($share['label']) ?>">

                                                        <i class="<?= $escaper->escapeHtmlAttr($share['icon_class']) ?>
                                                                <?= $escaper->escapeHtmlAttr($share['icon']) ?>"></i>

                                                    </a>
                                                <?php endif; ?>
                                            <?php endforeach; ?>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <?php endif; ?>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
        <?= $block->getPagerHtml() ?>
    <?php else : ?>
        <?= $escaper->escapeHtml(__('There are no posts at this moment')) ?>
    <?php endif; ?>
    <?= $block->getChildHtml('additional'); ?>
</div>
<style>
    .limiter {
        float: right;
    }
</style>
<?php elseif ($block->getBlogStyle() == '2'): ?>
<div class="post-list-content blog-layout" id="mbblog-list">
    <?php $postCollection = $block->getPostCollection(); ?>

    <?php if ($postCollection && $postCollection->getSize()) : ?>
        <?php
        $posts = [];
        foreach ($postCollection as $postItem) {
            $posts[] = $postItem;
        }

        $recommendCount = (int) $block->getRecommendCount();
        $recommendCount = $recommendCount > 0 ? $recommendCount : 3;

        $recommendedPosts = array_slice($posts, 0, $recommendCount);
        $heroPost = $recommendedPosts[0] ?? null;
        // Take remaining posts for mini grid - show all remaining in a responsive grid
        $miniPosts = array_slice($recommendedPosts, 1);
        ?>

        <section class="blog-recommended">
            <h2 class="blog-section-heading">
                <?= $escaper->escapeHtml(__('Recommended Posts')) ?>
            </h2>

            <?php if ($heroPost) : ?>
                <article class="blog-hero-card">
                    <a class="blog-hero-media" href="<?= $escaper->escapeUrl($heroPost->getUrl()) ?>">
                        <img
                            class="blog-hero-image"
                            src="<?= $escaper->escapeUrl($block->resizeImage($heroPost->getImage(), '1000x')) ?>"
                            alt="<?= $escaper->escapeHtmlAttr($heroPost->getName()) ?>"
                        />
                    </a>

                    <div class="blog-hero-content">
                        <h3 class="blog-hero-title">
                            <a class="post-link-title"
                               href="<?= $escaper->escapeUrl($heroPost->getUrl()) ?>"
                               title="<?= $escaper->escapeHtmlAttr($heroPost->getName()) ?>">
                                <?= $escaper->escapeHtml($heroPost->getName()) ?>
                            </a>
                        </h3>

                        <div class="blog-hero-meta">
                            <?=
                            /** @noEscape */
                            $block->getstyle2PostInfo($heroPost)
                            ?>
                        </div>
                    </div>
                </article>
            <?php endif; ?>

            <?php if (!empty($miniPosts)) : ?>
                <div class="blog-mini-grid">
                    <?php foreach ($miniPosts as $post) : ?>
                        <article class="blog-mini-card">
                            <?php if ($post->getImage()) : ?>
                                <a class="blog-mini-media" href="<?= $escaper->escapeUrl($post->getUrl()) ?>">
                                    <img
                                        class="blog-mini-image"
                                        src="<?= $escaper->escapeUrl($block->resizeImage($post->getImage(), '1000x')) ?>"
                                        alt="<?= $escaper->escapeHtmlAttr($post->getName()) ?>"
                                    />
                                </a>
                            <?php endif; ?>

                            <div class="blog-mini-content">
                                <h3 class="blog-mini-title">
                                    <a class="post-link-title"
                                       href="<?= $escaper->escapeUrl($post->getUrl()) ?>"
                                       title="<?= $escaper->escapeHtmlAttr($post->getName()) ?>">
                                        <?= $escaper->escapeHtml($post->getName()) ?>
                                    </a>
                                </h3>

                                <div class="blog-mini-meta">
                                    <?=
                                    /** @noEscape */
                                    $block->getstyle2PostInfo($post)
                                    ?>
                                </div>
                            </div>
                        </article>
                    <?php endforeach; ?>
                </div>
            <?php endif; ?>
        </section>

        <?php $featuredCategories = $block->getFeaturedCategories(); ?>
        <?php if ($featuredCategories) : ?>
            <?php
            $categoryIds = array_values(array_filter(array_map('trim', explode(',', $featuredCategories))));
            $categoryNames = $block->getCategoryNamesByIds($categoryIds);
            ?>

            <?php foreach ($categoryIds as $index => $categoryId) : ?>
                <?php $categoryPosts = $block->getPostsByCategoryIds([$categoryId]); ?>
                <?php if (!$categoryPosts || !$categoryPosts->getSize()) : ?>
                    <?php continue; ?>
                <?php endif; ?>

                <?php
                $categoryArray = [];
                foreach ($categoryPosts as $postItem) {
                    $categoryArray[] = $postItem;
                }

                $categoryTitle = $categoryNames[$index] ?? __('Featured Category');
                $leadPost = $categoryArray[0] ?? null;
                $listPosts = array_slice($categoryArray, 1, 3);
                ?>

                <section class="blog-category-section">
                    <div class="blog-category-header">
                        <h2 class="blog-category-heading">
                            <?= $escaper->escapeHtml($categoryTitle) ?>
                        </h2>

                        <?php $category = $block->helperData->getObjectByParam($categoryId, null, \Mavenbird\Blog\Helper\Data::TYPE_CATEGORY); ?>
                        <a class="blog-category-link" href="<?= $escaper->escapeUrl($block->helperData->getBlogUrl($category, \Mavenbird\Blog\Helper\Data::TYPE_CATEGORY)) ?>">
                            <?= $escaper->escapeHtml(__('See all posts')) ?>
                        </a>
                    </div>

                    <div class="blog-category-grid">
                        <?php if ($leadPost) : ?>
                            <article class="blog-category-feature">
                                <?php if ($leadPost->getImage()) : ?>
                                    <a class="blog-category-feature-media" href="<?= $escaper->escapeUrl($leadPost->getUrl()) ?>">
                                        <img
                                            class="blog-category-feature-image"
                                            src="<?= $escaper->escapeUrl($block->resizeImage($leadPost->getImage(), '1000x')) ?>"
                                            alt="<?= $escaper->escapeHtmlAttr($leadPost->getName()) ?>"
                                        />
                                    </a>
                                <?php endif; ?>

                                <div class="blog-category-feature-content">
                                    <h3 class="blog-category-feature-title">
                                        <a class="post-link-title"
                                           href="<?= $escaper->escapeUrl($leadPost->getUrl()) ?>"
                                           title="<?= $escaper->escapeHtmlAttr($leadPost->getName()) ?>">
                                            <?= $escaper->escapeHtml($leadPost->getName()) ?>
                                        </a>
                                    </h3>

                                    <?php if ($block->getShortDescription()) : ?>
                                        <div class="blog-category-feature-desc">
                                            <p>
                                                <?=
                                                /** @noEscape */
                                                $block->maxShortDescription($leadPost->getShortDescription(true))
                                                ?>
                                            </p>
                                        </div>
                                    <?php endif; ?>

                                     <div class="blog-category-feature-meta">
                                        <?=
                                        /** @noEscape */
                                        $block->getstyle2PostInfo($leadPost)
                                        ?>
                                    </div>
                                </div>
                            </article>
                        <?php endif; ?>

                        <div class="blog-category-list">
                            <?php foreach ($listPosts as $post) : ?>
                                <article class="blog-category-list-item">
                                    <h3 class="blog-category-list-title">
                                        <a class="post-link-title"
                                           href="<?= $escaper->escapeUrl($post->getUrl()) ?>"
                                           title="<?= $escaper->escapeHtmlAttr($post->getName()) ?>">
                                            <?= $escaper->escapeHtml($post->getName()) ?>
                                        </a>
                                    </h3>

                                    <div class="blog-category-list-meta">
                                        <?=
                                        /** @noEscape */
                                        $block->getstyle2PostInfo($post)
                                        ?>
                                    </div>
                                </article>
                            <?php endforeach; ?>
                        </div>
                    </div>
                </section>
            <?php endforeach; ?>
        <?php endif; ?>

        <?php $authorIds = $block->getAuthorIds(); ?>
        <?php if (!empty($authorIds)) : ?>
            <?php $featuredAuthors = $block->getAuthorsByIds($authorIds); ?>
            <?php if (!empty($featuredAuthors)) : ?>
                <section class="blog-featured-authors">
                    <h2 class="blog-section-heading">
                        <?= $escaper->escapeHtml(__('Featured Authors')) ?>
                    </h2>
                    <?php if ($block->isHyvaTheme()): ?>
                    <div id="featured-authors-splide" class="blog-authors-slider splide">
                        <div class="splide__track">
                            <ul class="splide__list">
                                <?php foreach ($featuredAuthors as $author) : ?>
                                    <li class="blog-author-card splide__slide">
                                        <a class="blog-author-link" href="<?= $escaper->escapeUrl($author->getUrl()) ?>">
                                            <?php if ($author->getImage()) : ?>
                                                <img 
                                                    class="blog-author-avatar"
                                                    src="<?= $escaper->escapeUrl($block->resizeImage($author->getImage(), '200x')) ?>"
                                                    alt="<?= $escaper->escapeHtmlAttr($author->getName()) ?>"
                                                />
                                            <?php else : ?>
                                                <img 
                                                    class="blog-author-avatar"
                                                    src="<?= $escaper->escapeUrl($block->getDefaultAuthorImage()) ?>"
                                                    alt="<?= $escaper->escapeHtmlAttr($author->getName()) ?>"
                                                />
                                            <?php endif; ?>
                                        </a>

                                            <h3 class="blog-author-name">
                                                <?= $escaper->escapeHtml($author->getName()) ?>
                                            </h3>
                                            
                                            <?php if ($author->getJobtitle()) : ?>
                                                <p class="blog-author-jobtitle">
                                                    <?= $escaper->escapeHtml($author->getJobtitle()) ?>
                                                </p>
                                            <?php endif; ?>
                                            
                                            <?php if ($author->getShortDescription()) : ?>
                                                <p class="blog-author-description">
                                                    <?= $block->maxShortDescription($author->getShortDescription()) ?>
                                                </p>
                                            <?php endif; ?>
                                            
                                            <div class="blog-author-social">
                                                <?php if ($author->getFacebookLink()) : ?>
                                                    <a href="<?= $escaper->escapeUrl($author->getFacebookLink()) ?>" target="_blank" rel="noopener" aria-label="Facebook">
                                                        <i class="fa-brands fa-facebook-f"></i>
                                                    </a>
                                                <?php endif; ?>
                                                <?php if ($author->getTwitterLink()) : ?>
                                                    <a href="<?= $escaper->escapeUrl($author->getTwitterLink()) ?>" target="_blank" rel="noopener" aria-label="Twitter">
                                                        <i class="fa-brands fa-x-twitter"></i>
                                                    </a>
                                                <?php endif; ?>
                                            </div>
                                    </li>
                                <?php endforeach; ?>
                            </ul>
                        </div>
                    </div>
                    <?php else: ?>
                    <div class="blog-authors-slider owl-carousel">
                        <?php foreach ($featuredAuthors as $author) : ?>
                            <div class="blog-author-card">
                                <a class="blog-author-link" href="<?= $escaper->escapeUrl($author->getUrl()) ?>">
                                    <?php if ($author->getImage()) : ?>
                                        <img 
                                            class="blog-author-avatar"
                                            src="<?= $escaper->escapeUrl($block->resizeImage($author->getImage(), '200x')) ?>"
                                            alt="<?= $escaper->escapeHtmlAttr($author->getName()) ?>"
                                        />
                                    <?php else : ?>
                                        <img 
                                            class="blog-author-avatar"
                                            src="<?= $escaper->escapeUrl($block->getDefaultAuthorImage()) ?>"
                                            alt="<?= $escaper->escapeHtmlAttr($author->getName()) ?>"
                                        />
                                    <?php endif; ?>
                                </a>

                                    <h3 class="blog-author-name">
                                        <?= $escaper->escapeHtml($author->getName()) ?>
                                    </h3>
                                    
                                    <?php if ($author->getJobtitle()) : ?>
                                        <p class="blog-author-jobtitle">
                                            <?= $escaper->escapeHtml($author->getJobtitle()) ?>
                                        </p>
                                    <?php endif; ?>
                                    
                                    <?php if ($author->getShortDescription()) : ?>
                                        <p class="blog-author-description">
                                            <?= $block->maxShortDescription($author->getShortDescription()) ?>
                                        </p>
                                    <?php endif; ?>
                                    
                                    <div class="blog-author-social">
                                        <?php if ($author->getFacebookLink()) : ?>
                                            <a href="<?= $escaper->escapeUrl($author->getFacebookLink()) ?>" target="_blank" rel="noopener" aria-label="Facebook">
                                                <i class="fa-brands fa-facebook-f"></i>
                                            </a>
                                        <?php endif; ?>
                                        <?php if ($author->getTwitterLink()) : ?>
                                            <a href="<?= $escaper->escapeUrl($author->getTwitterLink()) ?>" target="_blank" rel="noopener" aria-label="Twitter">
                                                <i class="fa-brands fa-x-twitter"></i>
                                            </a>
                                        <?php endif; ?>
                                    </div>
                            </div>
                        <?php endforeach; ?>
                    </div>
                    <?php endif; ?>
                </section>
                
                <?php if ($block->isHyvaTheme()): ?>
                <script>
                    document.addEventListener('DOMContentLoaded', function() {
                        const splideElement = document.getElementById('featured-authors-splide');
                        if (splideElement) {
                            new Splide('#featured-authors-splide', {
                                type: 'slide',
                                perPage: 2,
                                perMove: 1,
                                gap: '24px',
                                loop: false,
                                pagination: true,
                                arrows: true,
                                breakpoints: {
                                    768: {
                                        perPage: 1,
                                        gap: '16px'
                                    }
                                }
                            }).mount();
                        }
                    });
                </script>
                <?php else: ?>
                <script>
                    require(['jquery', 'owlCarousel'], function($) {
                        $(function() {
                            if ($('.blog-authors-slider').length) {
                                $('.blog-authors-slider').owlCarousel({
                                    loop: true,
                                    margin: 24,
                                    nav: true,
                                    dots: true,
                                    responsive: {
                                        0: {
                                            items: 1
                                        },
                                        768: {
                                            items: 2
                                        }
                                    }
                                });
                            }
                        });
                    });
                </script>
                <?php endif; ?>
            <?php endif; ?>
        <?php endif; ?>

    <?php else : ?>
        <div class="blog-empty-state">
            <?= $escaper->escapeHtml(__('There are no posts at this moment')) ?>
        </div>
    <?php endif; ?>

    <?= $block->getChildHtml('additional'); ?>
</div>

<style>
    #mbblog-list.blog-layout {
    width: 100%;
}

#mbblog-list .blog-section-heading,
#mbblog-list .blog-category-heading {
    margin: 0;
    font-size: 28px;
    line-height: 1.2;
    font-weight: 500;
    color: #111;
}

#mbblog-list .blog-recommended {
    margin-bottom: 34px;
}

#mbblog-list .blog-section-heading {
    margin-bottom: 14px;
}

#mbblog-list .blog-hero-card {
    display: grid;
    grid-template-columns: 1.15fr 1fr;
    gap: 0;
    overflow: hidden;
    border-radius: 16px;
    background: #e8ebff;
    min-height: 280px;
    margin-bottom: 12px;
}

#mbblog-list .blog-hero-media,
#mbblog-list .blog-mini-media,
#mbblog-list .blog-category-feature-media {
    display: block;
    text-decoration: none;
}

#mbblog-list .blog-hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

#mbblog-list .blog-hero-content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 26px 24px;
    background: #e8ebff;
}

#mbblog-list .blog-hero-title {
    margin: 0 0 12px;
    font-size: 30px;
    line-height: 1.25;
    font-weight: 700;
    color: #2d55d4;
}

#mbblog-list .blog-hero-title a,
#mbblog-list .blog-mini-title a,
#mbblog-list .blog-category-feature-title a,
#mbblog-list .blog-category-list-title a {
    color: inherit;
    text-decoration: none;
}

#mbblog-list .blog-hero-meta,
#mbblog-list .blog-mini-meta,
#mbblog-list .blog-category-feature-meta,
#mbblog-list .blog-category-list-meta {
    font-size: 12px;
    line-height: 1.4;
    color: #8b8b8b;
}

#mbblog-list .blog-mini-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 16px;
    margin-bottom: 34px;
}

#mbblog-list .blog-mini-card {
    overflow: hidden;
    border-radius: 16px;
}

#mbblog-list .blog-mini-image {
    width: 100%;
    height: 150px;
    object-fit: cover;
    display: block;
    border-radius: 16px;
}

#mbblog-list .blog-mini-content {
    padding-top: 10px;
}

#mbblog-list .blog-mini-title {
    margin: 0 0 6px;
    font-size: 18px;
    line-height: 1.3;
    font-weight: 500;
    color: #222;
}

#mbblog-list .blog-category-section {
    margin-top: 24px;
}

#mbblog-list .blog-category-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 14px;
}

#mbblog-list .blog-category-link {
    font-size: 14px;
    color: #777;
    text-decoration: none;
    white-space: nowrap;
}

#mbblog-list .blog-category-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.15fr) minmax(0, 1fr);
    gap: 24px;
    align-items: start;
}

#mbblog-list .blog-category-feature {
    display: block;
}

#mbblog-list .blog-category-feature-image {
    width: 100%;
    height: 190px;
    object-fit: cover;
    display: block;
    border-radius: 14px;
    margin-bottom: 10px;
}

#mbblog-list .blog-category-feature-title {
    margin: 0 0 8px;
    font-size: 20px;
    line-height: 1.35;
    font-weight: 500;
    color: #222;
}

#mbblog-list .blog-category-feature-desc {
    margin-top: 10px;
    font-size: 14px;
    line-height: 1.6;
    color: #8b8b8b;
}

#mbblog-list .blog-category-feature-desc p {
    margin: 0;
}

#mbblog-list .blog-category-list {
    display: flex;
    flex-direction: column;
    gap: 18px;
}

#mbblog-list .blog-category-list-item {
    padding-bottom: 14px;
    border-bottom: 1px solid #ededed;
}

#mbblog-list .blog-category-list-item:last-child {
    padding-bottom: 0;
    border-bottom: 0;
}

#mbblog-list .blog-category-list-title {
    margin: 0 0 6px;
    font-size: 18px;
    line-height: 1.35;
    font-weight: 500;
    color: #222;
}

#mbblog-list .blog-empty-state {
    padding: 20px 0;
    color: #666;
}

#mbblog-list .blog-featured-authors {
    margin-top: 48px;
}

#mbblog-list .blog-authors-slider {
    position: relative;
    padding: 0 3rem 2.5rem;
    margin: 0 auto;
    max-width: 1200px;
    margin-top: 24px;
}

#mbblog-list .blog-author-card {
    text-align: center;
    padding: 24px;
    border-radius: 16px;
    background: #ffffff;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    height: 100%;
}

#mbblog-list .blog-author-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.15);
}

#mbblog-list .blog-author-link {
    text-decoration: none;
    color: inherit;
    display: block;
}

#mbblog-list .blog-author-avatar {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    object-fit: cover;
    margin: 0 auto 1rem;
    border: 3px solid #f3f4f6;
}

#mbblog-list .blog-author-name {
    margin: 0 0 8px;
    font-size: 1.25rem;
    font-weight: 600;
    color: #1f2937;
}

#mbblog-list .blog-author-jobtitle {
    margin: 0 0 12px;
    font-size: 0.875rem;
    color: #6b7280;
    font-weight: 500;
}

#mbblog-list .blog-author-description {
    margin: 0 0 16px;
    font-size: 0.875rem;
    line-height: 1.5;
    color: #4b5563;
}

#mbblog-list .blog-author-social {
    display: flex;
    justify-content: center;
    gap: 1rem;
}

#mbblog-list .blog-author-social a {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: #f3f4f6;
    color: #374151;
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    transition: all 0.3s ease;
}

#mbblog-list .blog-author-social a:hover {
    background: #3b82f6;
    color: #ffffff;
}

#mbblog-list .blog-author-social i {
    font-size: 14px;
}

/* Splide Navigation */
#mbblog-list .blog-authors-slider .splide__arrow--prev,
#mbblog-list .blog-authors-slider .splide__arrow--next {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: #ffffff;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
    transition: all 0.3s ease;
    opacity: 1;
}

#mbblog-list .blog-authors-slider .splide__arrow--prev:hover,
#mbblog-list .blog-authors-slider .splide__arrow--next:hover {
    background: #3b82f6;
    box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
}

#mbblog-list .blog-authors-slider .splide__arrow--prev svg,
#mbblog-list .blog-authors-slider .splide__arrow--next svg {
    fill: #374151;
    transition: fill 0.3s ease;
}

#mbblog-list .blog-authors-slider .splide__arrow--prev:hover svg,
#mbblog-list .blog-authors-slider .splide__arrow--next:hover svg {
    fill: #ffffff;
}

#mbblog-list .blog-authors-slider .splide__arrow--prev {
    left: 10px;
}

#mbblog-list .blog-authors-slider .splide__arrow--next {
    right: 10px;
}

/* Splide Pagination */
#mbblog-list .blog-authors-slider .splide__pagination {
    bottom: 0;
}

#mbblog-list .blog-authors-slider .splide__pagination__page {
    width: 10px;
    height: 10px;
    background: #d1d5db;
    opacity: 1;
    transition: all 0.3s ease;
}

#mbblog-list .blog-authors-slider .splide__pagination__page.is-active {
    background: #3b82f6;
    transform: scale(1.2);
}

@media (max-width: 768px) {
    #mbblog-list .blog-hero-card,
    #mbblog-list .blog-category-grid {
        grid-template-columns: 1fr;
    }

    #mbblog-list .blog-mini-grid {
        grid-template-columns: 1fr;
    }

    #mbblog-list .blog-authors-slider {
        padding: 0 1rem 2.5rem;
    }
    
    #mbblog-list .blog-authors-slider .splide__arrow--prev,
    #mbblog-list .blog-authors-slider .splide__arrow--next {
        display: none;
    }

    #mbblog-list .blog-category-header {
        align-items: flex-start;
        flex-direction: column;
    }

    #mbblog-list .blog-hero-title {
        font-size: 24px;
    }
}
</style>
<?php endif; ?>